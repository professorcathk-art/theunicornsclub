# Neon Database Configuration
DATABASE_URL=postgresql://username:password@hostname/database
NEON_DATABASE_URL=postgresql://username:password@hostname/database

# API Configuration
API_SECRET_KEY=your_secret_key_here_make_it_long_and_random
NODE_ENV=production

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual Neon database connection string
# 3. Generate a secure API secret key
# 4. Never commit the .env file to version control
